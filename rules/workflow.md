# 워크플로우 원칙

## 0. 요구사항 구체화 (최우선)

모든 원칙에 앞서 적용한다. 사용자 명령이 들어오면 모호한 부분이 있는지 먼저 점검한다.

- 명령어의 범위, 대상, 기대 결과가 명확한가?
- 여러 해석이 가능한 부분이 있는가?
- 전제 조건이나 의존성이 빠져 있지 않은가?
- 모호하면 대화로 구체화한 뒤 실행한다. 추측하지 않는다.
- "알아서 해줘"라도 핵심 결정 포인트는 반드시 확인한다.

**구체화 방식 (clarify 패턴):**
- 추론 우선: 코드와 컨텍스트에서 답을 추론할 수 있으면 추론 결과를 제시하고 확인만 받는다
- 선택지 제공: 열린 질문 대신 선택지 + 추천 옵션을 제시한다
- 한 번에 최대 4개 질문: 질문이 많으면 라운드를 나눈다
- 결정 포인트만: 구현 세부사항이 아닌 핵심 결정 사항만 질문한다
- 이슈 기반 작업이면 `/clarify` 스킬 활용을 고려한다

## 1. 실행 전에 생각하기

사소하지 않은 모든 작업은 계획 모드로 시작한다 (3단계 이상이거나 구조적 결정이 필요한 경우).

- 뭔가 잘못되면 즉시 멈추고 다시 계획 세우기 — 그냥 밀어붙이지 말 것
- 모호함을 줄이려면 처음부터 상세 스펙을 작성할 것
- 가정을 명시적으로 밝힐 것. 확신이 없으면 물어볼 것
- 여러 해석이 가능하면 모두 제시할 것 — 조용히 하나만 고르지 말 것
- 더 단순한 접근법이 있다면 말할 것. 필요하면 반박할 것
- 뭔가 불명확하면 멈출 것. 뭐가 헷갈리는지 말하고 물어볼 것

## 2. 목표 기반 실행

성공 기준을 정의하라. 검증될 때까지 반복하라.

- 작업을 검증 가능한 목표로 변환한다:
  - "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
  - "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시킨다"
- 다단계 작업의 경우 간략한 계획을 밝힌다: `[단계] → 검증: [확인 방법]`
- 작동한다는 걸 증명하기 전까지 절대 작업 완료로 표시하지 말 것
- 테스트 실행, 로그 확인, 정확성 입증

## 3. 단순함 우선

문제를 해결하는 최소한의 코드. 추측성 코드는 금지.

- 요청받지 않은 기능은 추가하지 않는다
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다
- 불가능한 시나리오에 대한 에러 처리를 하지 않는다
- 200줄로 작성한 것이 50줄로 가능하다면, 다시 작성한다

스스로에게 물어보라: "시니어 엔지니어가 이걸 보고 과도하게 복잡하다고 할까?" 그렇다면 단순화한다.

## 4. 정밀한 변경

필요한 것만 수정한다. 자기가 만든 쓰레기만 치운다.

- 인접한 코드, 주석, 포맷팅을 "개선"하지 않는다
- 망가지지 않은 것을 리팩토링하지 않는다
- 기존 스타일에 맞춘다, 본인이라면 다르게 했을지라도
- 관련 없는 죽은 코드를 발견하면 언급만 하고 — 삭제하지 않는다
- 본인의 변경으로 사용되지 않게 된 import/변수/함수는 제거한다
- 검증 기준: 변경된 모든 줄이 사용자의 요청에 직접 연결되어야 한다

## 5. 분할 정복 및 서브에이전트 활용

메인 컨텍스트 창을 깔끔하게 유지하기 위해 서브에이전트를 적극 활용한다.

- 먼저 태스크를 최대한 잘게 나눌 것
- 조사, 탐색, 병렬 분석 등 독립적인 작업은 서브에이전트에 맡길 것
- 서브에이전트 하나당 하나의 작업만 — 집중 실행을 위해

## 6. 자기개선 루프

- 사용자에게 수정을 받을 때마다: 해당 패턴을 `docs/lessons/{제목}.md`에 기록
- 같은 실수를 반복하지 않도록 스스로 규칙을 작성
- 세션 시작 시 해당 프로젝트의 교훈 목록을 먼저 검토

## 7. 우아함 추구 (균형 있게)

- 사소하지 않은 변경이라면: 잠깐 멈추고 "더 우아한 방법이 있지 않을까?" 자문
- 수정이 어설프게 느껴진다면: 우아한 해결책을 구현
- 단순하고 명백한 수정엔 이 과정을 생략 — 오버엔지니어링 금지

## 8. 자율적인 버그 수정

- 버그 리포트를 받으면: 그냥 고칠 것. 하나하나 물어보지 말 것
- 로그, 에러, 실패한 테스트를 직접 찾아서 해결
- 어떻게 하라는 말 없이도 실패한 CI 테스트를 직접 가서 고칠 것
